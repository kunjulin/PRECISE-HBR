# Environment variables for local Docker development
# This file is loaded by docker-compose.yml

# --- Flask Configuration ---
# Set the environment to development to enable debug mode and other dev features
FLASK_ENV=development
FLASK_DEBUG=true

# --- SMART on FHIR Configuration for OpenEMR ---
# ❗ IMPORTANT: Replace with the Client ID from your OpenEMR instance
SMART_CLIENT_ID="YOUR_OPENEMR_CLIENT_ID_HERE"

# ❗ IMPORTANT: Replace with the Client Secret from your OpenEMR instance
SMART_CLIENT_SECRET="YOUR_OPENEMR_CLIENT_SECRET_HERE"

# The Redirect URI must match what you registered in OpenEMR for your local app
# This should point to your local machine where Docker is running
SMART_REDIRECT_URI="http://localhost:8080/callback"

# The full list of scopes required for the application to function
SMART_SCOPES="launch openid fhirUser profile user/Patient.rs user/Observation.rs user/Condition.rs user/MedicationRequest.rs user/Procedure.rs"

# --- Security Configuration ---
# A random secret key for signing session cookies.
# For local development, this can be any string.
FLASK_SECRET_KEY="a-super-secret-key-for-docker-dev"
